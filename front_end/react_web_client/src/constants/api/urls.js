/**
 * All endpoints
 */

// Host
const baseHost = 'http://localhost'
const basePort = 4557
const baseUrl = `${baseHost}:${basePort}`


// Categories:

// anonymization
export const urlAnonymizeData = `${baseUrl}/`